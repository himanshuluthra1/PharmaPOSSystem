<Window x:Class="PharmaPOS.WPF.Views.BarcodeCameraWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Scan barcode" Height="520" Width="720"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        FontFamily="{materialDesign:MaterialDesignFont}"
        Closing="Window_Closing">
    <DockPanel Margin="16">
        <DockPanel DockPanel.Dock="Top" Margin="0,0,0,12">
            <TextBlock x:Name="TitleText" FontSize="18" FontWeight="SemiBold" VerticalAlignment="Center" />
            <TextBlock DockPanel.Dock="Right" x:Name="StatusText" Opacity="0.65" VerticalAlignment="Center"
                       Text="Point camera at barcode..." />
        </DockPanel>
        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Browse image..." Style="{StaticResource MaterialDesignOutlinedButton}"
                    Click="Browse_Click" Margin="0,0,8,0" />
            <Button Content="Cancel" Style="{StaticResource MaterialDesignOutlinedButton}"
                    IsCancel="True" Click="Cancel_Click" />
        </StackPanel>
        <Border BorderBrush="{DynamicResource MaterialDesignDivider}" BorderThickness="1" Background="#111">
            <Image x:Name="PreviewImage" Stretch="Uniform" />
        </Border>
    </DockPanel>
</Window>
