<Window x:Class="PharmaPOS.WPF.Views.ReportBillsDrillDownWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Bills" Height="560" Width="860"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        FontFamily="{materialDesign:MaterialDesignFont}"
        PreviewKeyDown="Window_PreviewKeyDown">
    <DockPanel Margin="16">
        <DockPanel DockPanel.Dock="Top" Margin="0,0,0,10">
            <StackPanel>
                <TextBlock x:Name="TitleText" FontSize="18" FontWeight="SemiBold" TextWrapping="Wrap" />
                <TextBlock x:Name="SubtitleText" Opacity="0.65" Margin="0,4,0,0" TextWrapping="Wrap" />
            </StackPanel>
            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Top">
                <TextBlock Text="Enter / double-click opens bill · Esc closes"
                           Opacity="0.5" VerticalAlignment="Center" Margin="0,0,12,0" FontSize="11" />
                <Button Content="Close" Style="{StaticResource MaterialDesignRaisedButton}"
                        IsCancel="True" Click="CloseButton_Click" />
            </StackPanel>
        </DockPanel>

        <DataGrid x:Name="BillsGrid"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  HeadersVisibility="Column"
                  SelectionMode="Single"
                  SelectionUnit="FullRow"
                  GridLinesVisibility="All"
                  AlternatingRowBackground="#0A000000"
                  RowHeight="28"
                  Cursor="Hand"
                  MouseDoubleClick="BillsGrid_MouseDoubleClick"
                  PreviewKeyDown="BillsGrid_PreviewKeyDown">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Type" Binding="{Binding KindLabel}" Width="80" />
                <DataGridTextColumn Header="Invoice #" Binding="{Binding InvoiceNumber}" Width="130" />
                <DataGridTextColumn Header="Date" Binding="{Binding InvoiceDateLabel}" Width="140" />
                <DataGridTextColumn Header="Party" Binding="{Binding PartyName}" Width="*" MinWidth="120" />
                <DataGridTextColumn Header="Total" Binding="{Binding GrandTotal, StringFormat=N2}" Width="85" />
                <DataGridTextColumn Header="Paid" Binding="{Binding PaidAmount, StringFormat=N2}" Width="85" />
                <DataGridTextColumn Header="Due" Binding="{Binding BalanceDue, StringFormat=N2}" Width="85" />
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100" />
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</Window>
