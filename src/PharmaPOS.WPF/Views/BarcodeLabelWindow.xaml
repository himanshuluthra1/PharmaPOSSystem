<Window x:Class="PharmaPOS.WPF.Views.BarcodeLabelWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Barcode Label" Height="460" Width="520"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        FontFamily="{materialDesign:MaterialDesignFont}">
    <DockPanel Margin="16">
        <StackPanel DockPanel.Dock="Bottom" Margin="0,16,0,0">
            <Grid Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Margin="0,0,8,0">
                    <TextBlock Text="How many barcodes to print" FontSize="11" Opacity="0.7" Margin="0,0,0,2" />
                    <TextBox x:Name="QuantityBox"
                             Style="{StaticResource MaterialDesignOutlinedTextBox}"
                             Text="12"
                             materialDesign:HintAssist.IsFloating="False" />
                </StackPanel>
                <StackPanel Grid.Column="1" Margin="8,0,0,0">
                    <TextBlock Text="How many on 1 A4 page" FontSize="11" Opacity="0.7" Margin="0,0,0,2" />
                    <TextBox x:Name="PerPageBox"
                             Style="{StaticResource MaterialDesignOutlinedTextBox}"
                             Text="12"
                             materialDesign:HintAssist.IsFloating="False" />
                </StackPanel>
            </Grid>
            <TextBlock x:Name="LayoutHint" FontSize="11" Opacity="0.55" Margin="0,0,0,12"
                       Text="Example: 12 barcodes, 12 per page = 1 A4 sheet in a grid." />
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Print" Style="{StaticResource MaterialDesignRaisedButton}" Click="Print_Click" Margin="0,0,8,0" />
                <Button Content="Close" Style="{StaticResource MaterialDesignOutlinedButton}" Click="Close_Click" IsCancel="True" />
            </StackPanel>
        </StackPanel>

        <Border Background="White" Padding="24" BorderBrush="#DDD" BorderThickness="1">
            <StackPanel HorizontalAlignment="Center">
                <TextBlock x:Name="TitleText" FontSize="16" FontWeight="SemiBold"
                           TextAlignment="Center" TextWrapping="Wrap" Margin="0,0,0,12"
                           Foreground="Black" />
                <Image x:Name="BarcodeImage" Height="120" Stretch="Uniform" />
                <TextBlock x:Name="BarcodeText" FontSize="14" FontFamily="Consolas"
                           TextAlignment="Center" Margin="0,10,0,0" Foreground="Black" />
            </StackPanel>
        </Border>
    </DockPanel>
</Window>
