<Window x:Class="PharmaPOS.WPF.Views.MedicineLedgerPopupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Medicine ledger" Height="560" Width="920"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        FontFamily="{materialDesign:MaterialDesignFont}"
        PreviewKeyDown="Window_PreviewKeyDown">
    <DockPanel Margin="16">
        <DockPanel DockPanel.Dock="Top" Margin="0,0,0,10">
            <StackPanel>
                <TextBlock x:Name="TitleText" FontSize="18" FontWeight="SemiBold" TextWrapping="Wrap" />
                <TextBlock x:Name="SubtitleText" Opacity="0.65" Margin="0,4,0,0" TextWrapping="Wrap" />
            </StackPanel>
            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Top">
                <TextBlock Text="Esc / Ctrl+L to close" Opacity="0.5" VerticalAlignment="Center" Margin="0,0,12,0" FontSize="11" />
                <Button Content="Close" Style="{StaticResource MaterialDesignRaisedButton}"
                        IsDefault="True" Click="CloseButton_Click" />
            </StackPanel>
        </DockPanel>

        <DataGrid x:Name="LedgerGrid"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal"
                  AlternatingRowBackground="#0A000000"
                  RowHeight="28">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Date" Binding="{Binding MovementDateLabel}" Width="150" />
                <DataGridTextColumn Header="Type" Binding="{Binding MovementTypeLabel}" Width="120" />
                <DataGridTextColumn Header="Batch" Binding="{Binding BatchNumber}" Width="100" />
                <DataGridTextColumn Header="Qty" Binding="{Binding Quantity, StringFormat=0.##}" Width="70" />
                <DataGridTextColumn Header="Balance" Binding="{Binding BalanceAfter, StringFormat=0.##}" Width="80" />
                <DataGridTextColumn Header="Unit cost" Binding="{Binding UnitCost, StringFormat=N2}" Width="80" />
                <DataGridTextColumn Header="Reference" Binding="{Binding ReferenceNumber}" Width="120" />
                <DataGridTextColumn Header="Remarks" Binding="{Binding Remarks}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</Window>
