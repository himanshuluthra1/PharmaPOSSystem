<Window x:Class="PharmaPOS.WPF.Views.MedicineSearchWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Search Medicine" Height="480" Width="560"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        FontFamily="{materialDesign:MaterialDesignFont}"
        PreviewKeyDown="Window_PreviewKeyDown">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Search Medicine" FontSize="18" FontWeight="Bold" Margin="0,0,0,12" />

        <TextBox x:Name="SearchBox" Grid.Row="1"
                 materialDesign:HintAssist.Hint="Type medicine name (min 2 chars)"
                 Style="{StaticResource MaterialDesignOutlinedTextBox}"
                 Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                 PreviewKeyDown="SearchBox_PreviewKeyDown"
                 Margin="0,0,0,8" />

        <TextBlock Grid.Row="2" Text="{Binding Hint}" FontSize="12" Opacity="0.7"
                   Margin="0,0,0,6" VerticalAlignment="Top"
                   Visibility="{Binding Hint, Converter={StaticResource StringToVisibility}}" />

        <ListBox x:Name="ResultsList" Grid.Row="2" Margin="0,28,0,0"
                 ItemsSource="{Binding Results}"
                 SelectedIndex="{Binding SelectedIndex, Mode=TwoWay}"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 Focusable="False"
                 PreviewKeyDown="ResultsList_PreviewKeyDown"
                 MouseDoubleClick="ResultsList_MouseDoubleClick">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <DockPanel Margin="2">
                        <TextBlock Text="{Binding TotalStock, StringFormat='Stock: {0:0.##}'}"
                                   DockPanel.Dock="Right" Opacity="0.6" FontSize="11"
                                   VerticalAlignment="Center" />
                        <StackPanel>
                            <TextBlock Text="{Binding Name}" FontWeight="SemiBold" />
                            <TextBlock Text="{Binding GenericName}" FontSize="11" Opacity="0.6" />
                        </StackPanel>
                    </DockPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <TextBlock Text="↑↓ navigate  •  Enter select  •  Esc cancel" Opacity="0.5"
                       VerticalAlignment="Center" Margin="0,0,16,0" FontSize="11" />
            <Button Content="Cancel" Style="{StaticResource MaterialDesignOutlinedButton}"
                    Margin="0,0,8,0" IsCancel="True" />
            <Button Content="Select" Style="{StaticResource MaterialDesignRaisedButton}"
                    IsDefault="True" Click="SelectButton_Click" />
        </StackPanel>
    </Grid>
</Window>
