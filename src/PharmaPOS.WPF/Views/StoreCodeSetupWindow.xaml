<Window x:Class="PharmaPOS.WPF.Views.StoreCodeSetupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Store activation"
        Height="460" Width="520"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        ShowInTaskbar="True"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        FontFamily="{materialDesign:MaterialDesignFont}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="Activate this store PC" FontSize="20" FontWeight="Bold" />
            <TextBlock Margin="0,8,0,0" TextWrapping="Wrap" Opacity="0.75"
                       Text="Choose any store code you like (shown in the app). A unique Store ID is created automatically and used for online reporting. Activation is locked to this PC after vendor approval." />
        </StackPanel>

        <TextBlock Grid.Row="1" Margin="0,18,0,4" FontWeight="SemiBold" Text="This PC Machine ID" />
        <DockPanel Grid.Row="2" Margin="0,0,0,8">
            <Button DockPanel.Dock="Right" Margin="8,0,0,0"
                    Style="{StaticResource MaterialDesignOutlinedButton}"
                    Content="Copy" Click="CopyMachineId_Click" />
            <TextBox x:Name="MachineIdBox" IsReadOnly="True" FontFamily="Consolas" FontSize="13"
                     TextWrapping="Wrap" />
        </DockPanel>

        <TextBlock Grid.Row="3" Margin="0,8,0,4" FontWeight="SemiBold" Text="Your store code *" />
        <TextBox Grid.Row="4"
                 x:Name="StoreCodeBox"
                 materialDesign:HintAssist.Hint="e.g. My Pharmacy / DEL-01"
                 FontSize="18"
                 FontWeight="SemiBold"
                 MaxLength="40" />

        <TextBlock Grid.Row="5" x:Name="StatusText" Margin="0,12,0,0"
                   TextWrapping="Wrap" Opacity="0.9" />

        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button x:Name="ActivateButton"
                    Content="Activate"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Padding="24,8"
                    IsDefault="True"
                    Click="Activate_Click" />
        </StackPanel>
    </Grid>
</Window>
