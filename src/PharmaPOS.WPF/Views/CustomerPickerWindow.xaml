<Window x:Class="PharmaPOS.WPF.Views.CustomerPickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Search Customer" Height="640" Width="960"
        MinHeight="520" MinWidth="800"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        FontFamily="{materialDesign:MaterialDesignFont}"
        PreviewKeyDown="Window_PreviewKeyDown">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Search Customer" FontSize="18" FontWeight="Bold" Margin="0,0,0,10" />

        <TextBox x:Name="SearchBox" Grid.Row="1"
                 materialDesign:HintAssist.Hint="Name or mobile"
                 Style="{StaticResource MaterialDesignOutlinedTextBox}"
                 Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                 Margin="0,0,0,8" />

        <DockPanel Grid.Row="2">
            <TextBlock DockPanel.Dock="Top" Text="{Binding Hint}" FontSize="12" Opacity="0.7"
                       Margin="0,0,0,6"
                       Visibility="{Binding Hint, Converter={StaticResource StringToVisibility}}" />
            <DataGrid x:Name="ResultsList"
                      ItemsSource="{Binding Results}"
                      SelectedItem="{Binding Selected}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      CanUserAddRows="False"
                      HeadersVisibility="Column"
                      SelectionMode="Single"
                      SelectionUnit="FullRow"
                      GridLinesVisibility="Horizontal"
                      PreviewKeyDown="ResultsList_PreviewKeyDown"
                      MouseDoubleClick="ResultsList_MouseDoubleClick">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="2*" MinWidth="160" />
                    <DataGridTextColumn Header="Mobile Number" Binding="{Binding Phone}" Width="130" />
                    <DataGridTextColumn Header="Due Amount" Binding="{Binding OutstandingBalance, StringFormat=N2}" Width="110" />
                    <DataGridTextColumn Header="Address" Binding="{Binding Address}" Width="3*" MinWidth="200" />
                </DataGrid.Columns>
            </DataGrid>
        </DockPanel>

        <DockPanel Grid.Row="3" Margin="0,12,0,0" LastChildFill="False">
            <TextBlock DockPanel.Dock="Left" Text="↑↓ navigate  •  Enter select  •  Esc cancel"
                       Opacity="0.5" VerticalAlignment="Center" FontSize="11" />
            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                <Button Content="Cancel" Command="{Binding CancelCommand}"
                        Style="{StaticResource MaterialDesignOutlinedButton}" Margin="0,0,8,0" IsCancel="True" />
                <Button Content="Select" Command="{Binding SelectCommand}"
                        Style="{StaticResource MaterialDesignRaisedButton}" IsDefault="True" />
            </StackPanel>
        </DockPanel>
    </Grid>
</Window>
