<Window x:Class="PharmaPOS.WPF.Views.PendingMappingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Queued mappings" Height="480" Width="900"
        WindowStartupLocation="CenterOwner"
        MinHeight="360" MinWidth="720"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        FontFamily="{materialDesign:MaterialDesignFont}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Queued mappings (not saved yet)" FontSize="18" FontWeight="Bold" />
        <TextBlock Grid.Row="1" Opacity="0.7" Margin="0,4,0,12" TextWrapping="Wrap"
                   Text="These links are held in memory until you click Apply Mappings on the main screen. Remove any incorrect row before saving." />

        <DataGrid Grid.Row="2" ItemsSource="{Binding PendingMappings}"
                  SelectedItem="{Binding SelectedPendingMapping, Mode=TwoWay}"
                  AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                  HeadersVisibility="Column" SelectionMode="Single"
                  ScrollViewer.CanContentScroll="False"
                  ScrollViewer.VerticalScrollBarVisibility="Auto"
                  ScrollViewer.HorizontalScrollBarVisibility="Auto">
            <DataGrid.Columns>
                <DataGridTextColumn Header="MedWin" Binding="{Binding MedWinName}" Width="*" MinWidth="140" />
                <DataGridTextColumn Header="MedWin ID" Binding="{Binding MedWinExternalId}" Width="90" />
                <DataGridTextColumn Header="OneMG" Binding="{Binding OneMgName}" Width="*" MinWidth="140" />
                <DataGridTextColumn Header="OneMG Pack" Binding="{Binding OneMgPackInfo}" Width="140" MinWidth="100" />
                <DataGridTextColumn Header="OneMG ID" Binding="{Binding OneMgExternalId}" Width="90" />
                <DataGridTemplateColumn Header="" Width="88">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Remove"
                                    Style="{StaticResource MaterialDesignOutlinedButton}"
                                    Padding="8,2"
                                    Command="{Binding DataContext.RemovePendingMappingCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                    CommandParameter="{Binding}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <DockPanel Grid.Row="3" Margin="0,12,0,0">
            <Button DockPanel.Dock="Left" Content="Clear all" Style="{StaticResource MaterialDesignOutlinedButton}"
                    Command="{Binding ClearPendingCommand}" />
            <Button DockPanel.Dock="Right" Content="Close" Style="{StaticResource MaterialDesignRaisedButton}"
                    IsDefault="True" Click="CloseButton_Click" MinWidth="90" />
        </DockPanel>
    </Grid>
</Window>
